#!upstart
description "progress-filesystems-mounted"

start on runlevel 6 and stopped xbmc-exits

task

script

	[ -e /run/nosplash ] && exit 0

	splash --msgtxt="rebooting..." --infinitebar --flip || true

end script
