start on started portmap

pre-start exec sm-notify

script
        if [ -f "$DEFAULTFILE" ]; then
            . "$DEFAULTFILE"
        fi
    
        [ "x$NEED_STATD" != xno ] || exit 0

        exec rpc.statd -L $STATDOPTS
end script

