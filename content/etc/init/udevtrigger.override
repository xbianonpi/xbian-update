start on started udev

script
    set +e
    udevadm trigger --action=add --subsystem-nomatch=net
    udevadm trigger --action=add --sysname-match=wlan0
    udevadm trigger --action=add --sysname-match=wlan1
end script
